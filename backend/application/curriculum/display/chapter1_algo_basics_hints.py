"""Pedagogical hints for chapter 1 algo_basics (pattern-keyed, language-neutral).

Hints focus on transfer pitfalls and debugging strategy — not a restatement of the condition.
"""

from __future__ import annotations

CHAPTER1_HINTS: dict[str, list[str]] = {
    "task_001": [
        "Лучший результат держите в отдельной переменной: обновляйте её только когда текущее значение строго больше.",
        "Не обнуляйте «лучшее» внутри цикла — иначе потеряете уже найденный максимум.",
        "Границы цикла в целевом языке могут начинаться не с нуля — сверьте с эталоном слева.",
    ],
    "task_002": [
        "Ответ — позиция с единицы, а не индекс с нуля: первый элемент — это 1, не 0.",
        "Запоминайте только первое совпадение: после присвоения позиции не перезаписывайте её.",
        "Два числа в первой строке входа читаются одним приёмом — не разбивайте на два отдельных чтения, если формат теста это не позволяет.",
    ],
    "task_004": [
        "Сначала устраните синтаксические ошибки и чужие конструкции, затем проверяйте логику счётчика.",
        "Ноль и отрицательные значения не должны увеличивать счётчик — условие строго «больше нуля».",
        "Стартовое значение счётчика влияет на ответ даже при верном условии в цикле.",
    ],
    "task_005": [
        "Сумму накапливайте в одной переменной до конца цикла — не выводите промежуточные значения.",
        "Количество итераций должно совпасть с числом введённых значений: проверьте начало и конец диапазона цикла.",
        "В целевом языке тот же алгоритм часто записывают с другой стартовой границей счётчика.",
    ],
    "task_006": [
        "Сначала соберите полную сумму, делите один раз в конце — не усредняйте на каждом шаге.",
        "Нужна целая часть частного: оператор деления в целевом языке может отличаться от привычного `/` или `//`.",
        "Инициализация накопителя и старт цикла задают, все ли элементы попадут в сумму.",
    ],
    "task_007": [
        "Порог «не меньше 50» включает само значение 50 — строгое «больше» даст неверный ответ на границе.",
        "Сначала добейтесь компиляции: в коде могут быть следы другого языка, не только одна логическая ошибка.",
        "Счётчик увеличивайте внутри ветки, когда условие выполняется, а не после цикла.",
    ],
    "task_008": [
        "Три метрики выводите в одной строке через пробел — отдельные строки вывода тест не примет.",
        "Среднее — целая часть от деления суммы на количество, без округления вверх.",
        "Максимум, пороговый подсчёт и накопление суммы — приёмы из задач 1, 6 и 7 этой главы; баннеров здесь нет намеренно.",
    ],
    "task_129": [
        "Последняя цифра — остаток от деления на 10; убрать цифру — целочисленное деление на 10.",
        "Отрицательное входное число обработайте до цикла по цифрам, иначе остаток может вести себя иначе.",
        "Накопитель суммы цифр обнуляйте до цикла и не смешивайте операторы остатка и деления.",
    ],
}


def chapter1_hints_for_pattern(pattern_key: str | None) -> list[str]:
    key = str(pattern_key or "").strip()
    raw = CHAPTER1_HINTS.get(key) or []
    return [str(item).strip() for item in raw if str(item).strip()]
