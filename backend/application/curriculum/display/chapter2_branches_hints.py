"""Pedagogical hints for chapter 2 branches (pattern-keyed, language-neutral)."""

from __future__ import annotations

CHAPTER2_HINTS: dict[str, list[str]] = {
    "task_009": [
        "Держите «лучшее» в одной переменной и обновляйте только при строгом > — при равенстве ответ не меняется.",
        "Три числа обычно приходят одной строкой; не разбивайте ввод, если тесты дают «a b c».",
        "Различайте присваивание и сравнение в целевом языке — см. ожидаемую конструкцию «Условия и ветвление».",
    ],
    "task_010": [
        "Проверяйте пороги сверху вниз: t < 0, затем t ≤ 25, затем t ≤ 35 — иначе более широкая ветка сработает раньше.",
        "После выбора ветки выведите ровно одну метку: freezing, normal, hot или danger — в нижнем регистре.",
        "Для температуры нужна цепочка взаимоисключающих веток — пример else if смотрите в «Условия и ветвление».",
    ],
    "task_013": [
        "Сначала отсечь invalid (< 0 или > 100), затем идти по шкале сверху: ≥90, ≥70, ≥50, иначе retry.",
        "Граница 50 включена в satisfactory (≥50); 40 баллов — это retry, не invalid.",
        "Одно число на входе, одно слово на выходе — без пояснений и без нескольких строк.",
    ],
    "task_011": [
        "Некорректность проверяйте до типа: если a+b ≤ c (или любая перестановка) — сразу invalid.",
        "Равносторонний случай — когда равны все три стороны; две равные при третьей другой — isosceles.",
        "В отладке сначала уберите синтаксис чужого языка, затем порядок веток: invalid → equilateral → isosceles → scalene.",
    ],
    "task_012": [
        "Високосность: кратен 400, или (кратен 4 и не кратен 100); одного «делится на 4» мало для 1900/2100.",
        "Число дней месяца определите до проверки d: 31/30/28/29 — отдельные ветки по m.",
        "d, m, y — одна строка входа; формула года и mod/% — в «Арифметика и выражения», ввод — в «Ввод и вывод».",
    ],
    "task_014": [
        "Зима — месяцы 12, 1, 2; в Pascal их объединяют в одной ветке case, в Python — через проверку нескольких значений.",
        "Месяц вне 1..12 → invalid; не подбирайте «ближайший» сезон.",
        "В C++/C# после каждого case нужен break — пример в «Выбор по значению».",
    ],
    "task_015": [
        "Базовая скидка — одна ступень по сумме (≥20000 → 25%, ≥10000 → 15%, …), не сумма нескольких if.",
        "Бонусы +5 (student) и +3 (coupon) прибавляются к базе, но итог ограничен 30%.",
        "Флаги только 0 или 1; при total < 0 или неверном флаге — invalid без расчёта процента.",
    ],
    "task_016": [
        "accepted требует выполнения всех четырёх условий сразу; review — более мягкий порог, но тоже age ≥ 18.",
        "Сравнение долга: debt * 10 ≤ income * 4 (accepted) или * 6 (review) — не теряйте коэффициенты при переносе.",
        "Итоговая без проактивных баннеров: соберите цепочки if/else if из задач 10, 13 и 15 этой главы.",
    ],
}


def chapter2_hints_for_pattern(pattern_key: str | None) -> list[str]:
    key = str(pattern_key or "").strip()
    raw = CHAPTER2_HINTS.get(key) or []
    return [str(item).strip() for item in raw if str(item).strip()]
