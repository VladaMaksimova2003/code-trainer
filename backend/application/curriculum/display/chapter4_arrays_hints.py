"""Pedagogical hints for chapter 4 arrays (pattern-keyed, language-neutral)."""

from __future__ import annotations

CHAPTER4_HINTS: dict[str, list[str]] = {
    "task_025": [
        "Выведите элементы в обратном порядке: последний прочитанный — первым в ответе.",
        "n задаёт и длину массива, и число строк ввода — каждое число на отдельной строке после n.",
        "Все числа в одной строке через пробел; лишний пробел в конце может сломать тест.",
    ],
    "task_026": [
        "Первая строка — два числа n и k; элементы массива читаются построчно после неё.",
        "Перед сдвигом приведите k по модулю n; при k = 0 массив не меняется.",
        "Циклический сдвиг вправо на k: последние k элементов оказываются в начале.",
    ],
    "task_027": [
        "Позиция pos в условии 1-based: первый элемент массива имеет pos = 1.",
        "После удаления сдвиньте хвост влево; в Python это del a[pos − 1] или срез.",
        "При pos вне 1..n — invalid; удаление единственного элемента даёт пустой вывод.",
    ],
    "task_028": [
        "Позиция pos и значение x часто приходят в одной строке после элементов массива.",
        "Допустимый pos — от 1 до n + 1; за пределами выведите invalid.",
        "Вставка на pos раздвигает элементы с этой позиции: в Python insert(pos − 1, x).",
    ],
    "task_029": [
        "Сначала n и n чисел первого массива, затем m и m чисел второго — порядок строк важен.",
        "Результат — все элементы первого, затем все второго, в одной строке через пробел.",
        "При m = 0 второй массив пуст — выведите только первый без лишних пробелов в конце.",
    ],
    "task_030": [
        "Ответ строго yes или no в нижнем регистре — не True/False и не duplicate/unique.",
        "Достаточно найти одну пару одинаковых элементов с разными индексами.",
        "Сравнивайте пары (i, j) при j > i — соседние одинаковые числа тоже дают yes.",
    ],
    "task_031": [
        "В первой строке n и m; далее две строки отсортированных массивов A и B.",
        "Слияние двумя указателями: на каждом шаге берите меньший из текущих элементов.",
        "При равенстве a[i] и b[j] сначала берите из A; допишите хвост неслитого массива.",
    ],
    "task_032": [
        "Массив уже отсортирован — найдите позицию вставки x, сохраняя порядок.",
        "Ответ — номер позиции с единицы: куда встанет x, если вставить его в массив.",
        "Итоговая без проактивных баннеров: соберите 1-based индексацию и сравнение с элементами.",
    ],
}


def chapter4_hints_for_pattern(pattern_key: str | None) -> list[str]:
    key = str(pattern_key or "").strip()
    raw = CHAPTER4_HINTS.get(key) or []
    return [str(item).strip() for item in raw if str(item).strip()]
